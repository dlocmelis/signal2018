test: case
name: calls CRUD json
description: <insert description of test case>
casedetails:
  vars:
    $description: $genString(6)
    $destination: $genString(7)
    $id: $genString(8)
    $name: $genString(6)
    $source: $genString(9)
    $url: $genString(5)
  defaultCollection: StoreAPI
  steps:
  - name: createCall json
  - name: getCall json
  - name: updateCall json
  - name: deleteCall json
