test: case
name: calls CRUD json
description: <insert description of test case>
casedetails:
  vars:
    $description: $genString(9)
    $destination: $genString(6)
    $id: $genString(6)
    $name: $genString(9)
    $source: $genString(7)
    $url: $genString(6)
  defaultCollection: StoreAPI
  steps:
  - name: createCall json
  - name: getCall json
  - name: updateCall json
  - name: deleteCall json
