test: case
name: bananas CRUD json
description: <insert description of test case>
casedetails:
  vars:
    $calories: $genInteger(44,251)
    $color: $genString(6)
    $id: $genString(9)
    $isRipe: $genString(6)
    $length: $genString(5)
    $name: $genString(8)
  defaultCollection: StoreAPI
  steps:
  - name: createBanana json
  - name: getBanana json
  - name: updateBanana json
  - name: deleteBanana json
